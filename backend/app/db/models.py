"""Database model documentation placeholders.

The MVP backend uses Python's built-in sqlite3 module to avoid adding runtime
installation steps during the migration. SQLAlchemy/Alembic models can replace
these table definitions once the API contracts stabilize.
"""
